const User = require('./User');
const Product = require('./Comment');
const Category = require('./Category');
const Post = require('./Post')
const Order = require('./Order');

module.exports = { User, Product, Category, Order, Post };
